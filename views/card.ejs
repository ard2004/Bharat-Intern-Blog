<!DOCTYPE html>
<html lang="en">
    <head>
        <title></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <%- include('./cardStyle.ejs') %>
        <% for(let i = 0; i < 5; i++){ %>
            <div id="card">
                <div id="blogTitle">
                    <%= blog_title %>
                </div>
                <div id="blogBody">
                    <%= blog_body %>
                </div>
            </div>
        <% } %>
    </body>
</html>
